#Web Interface for HeadlineGenerator

1. copy .php and .html files on server along with all other python scripts

2. the headlin.php file generates 'article.txt', 'summary.txt' and 'headline.txt' when invoked by corresponding AJAX requests